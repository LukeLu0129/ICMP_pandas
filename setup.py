from setuptools import find_packages, setup

setup(
    name='icmp_pandas',
    packages=find_packages(),
    version='0.1.0',
    description='A python library to read ICM+ related files as pandas object (pd.DataFrame or pd.Series)',
    author='Kuan-Ying (Luke) Lu',
)